openapi: "3.0.1"
info:
  title: Monty API
  description: >-
    This API contains CRUD methods to manage knights in a kingdom.
    To use this API, you must register a free account at
    https://monty.cloud.example.com/account.
  version: "1.0"
servers:
- url: https://monty.cloud.example.com/rest/v1
tags:
- name: Knights
  description: Manage knights
paths:
  /{kingdomId}/knights:
    get:
      tags:
      - Knights
      summary: Gets the list of all existing knights in a kingdom. The response does not include the previously deleted knignts.
      operationId: knightsIndex
      parameters:
      - $ref: '#/components/parameters/kingdomIdInPath'
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RestListOfKnights'
    post:
      tags:
      - Knights
      summary: Creates a knight with the specified parameters.
      operationId: knightsCreate
      parameters:
      - $ref: '#/components/parameters/kingdomIdInPath'
      requestBody:
        $ref: '#/components/requestBodies/createKnight'
      responses:
        201:
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RestKnight'
  /{kingdomId}/knights/{knightId}:
    get:
      tags:
      - Knights
      summary: Gets the details of the specified knight.
      operationId: knightsShow
      parameters:
      - $ref: '#/components/parameters/kingdomIdInPath'
      - $ref: '#/components/parameters/knightIdInPath'
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RestKnight'
    put:
      tags:
      - Knights
      summary: Updates the specified knight.
      operationId: knightsUpdate
      parameters:
      - $ref: '#/components/parameters/kingdomIdInPath'
      - $ref: '#/components/parameters/knightIdInPath'
      requestBody:
        $ref: '#/components/requestBodies/editKnight'
      responses:
        202:
          description: Accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RestKnight'
    delete:
      tags:
      - Knights
      summary: Deletes the specified knight.
      operationId: knightsDelete
      parameters:
      - $ref: '#/components/parameters/kingdomIdInPath'
      - $ref: '#/components/parameters/knightIdInPath'
      responses:
        204:
          description: No Content
          content:
            application/json:
              schema:
                type: string
              example: {}
components:
  schemas:
    RestListOfKnights:
      type: array
      items:
        $ref: '#/components/schemas/RestKnight'
    RestKnight:
      type: object
      properties:
        id:
          type: string
          description: >-
            A globally unique identifier consisting of latin characters, digits, and hyphens.
            ID is created automatically when a knight is created.
            ID cannot be created or changed manually.
          format: uuid
          example: b8b71701-4811-46c3-a0d6-cc7827cef1fd
        name:
          pattern: (^Sir|^S1r)\s[A-Za-z0-9]{1,252}
          type: string
          description: >-
            A name of the knight.
            The name length should be between 5 and 256 characters long.
            The name is not a unique property, so two or more knights can have the same name.
            The name can be changed at any time.
          example: Sir Brunswick
        age:
          maximum: 65 
          minimum: 18
          type: integer
          description: >-
            The knight age in years.
            The age can be increased at any time, but cannot be decreased.
          format: int32
          example: 20
        favoriteColor:
          type: string
          description: >-
            The favorite color of the knight.
            The color can be set once only, it cannot be changed later.
          enum:
          - RED
          - GREEN
          - BLUE
        bestFriend:
          type: string
          description: >-
            The knight's best friend. It can only be ID of an existing knight.
            All the shrubberies of the best friend are copied to the shrubberies'
            array of the knight and stays in the array even if the best friend
            is deleted from the kingdom.
            Once the best friend is set, this parameter cannot be changed
            unless the best friend is deleted from the kingdom.
            When the best friend is deleted from the kingdom, this parameter automatically
            becomes empty and a new best friend can be set.
          format: uuid
          example: 1dae11c2-915a-43b8-9511-11036934728d
        shrubberies:
          type: array
          description: >-
            The knight's shrubberries. Once a knight is created,
            it is automatically assigned one shrubbery with randomized values.
            Every time the 'bestFriend' parameter is set, the shrubberies array
            is extended with the best friend's shrubberies.
            The shrubberies array cannot be extended manually.
          items:
            $ref: '#/components/schemas/RestShrubbery'
    RestShrubbery:
      type: object
      properties:
        height:
          minimum: -1 # TODO
          type: number
          description: 'TODO'
          format: '' # TODO
          example: -1 # TODO
        radius:
          minimum: -1 # TODO
          type: number
          description: 'TODO'
          format: '' # TODO
          example: -1 # TODO
        volume:
          minimum: -1 # TODO
          type: number
          description: 'TODO'
          format: '' # TODO
          example: -1 # TODO
        description:
          pattern: 'TODO'
          type: string
          description: 'TODO'
          example: 'TODO'
        price:
          minimum: -1 # TODO
          type: integer
          description: 'TODO'
          format: '' # TODO
          example: -1 # TODO
    RestCreateKnight:
      type: object
      properties:
        name:
          $ref: '#/components/schemas/RestKnight/properties/name'
        age:
          $ref: '#/components/schemas/RestKnight/properties/age'
        favoriteColor:
          $ref: '#/components/schemas/RestKnight/properties/favoriteColor'
        bestFriend:
          $ref: '#/components/schemas/RestKnight/properties/bestFriend'
        schrubberies:
          type: array
          items:
            $ref: '#/components/schemas/RestCreateShrubbery'
    RestEditKnight:
      type: object
      properties:
        name:
          $ref: '#/components/schemas/RestKnight/properties/name'
        age:
          $ref: '#/components/schemas/RestKnight/properties/age'
        bestFriend:
          $ref: '#/components/schemas/RestKnight/properties/bestFriend'
        shrubberies:
          type: array
          items:
            $ref: '#/components/schemas/RestEditShrubbery'
    RestCreateShrubbery:
      type: object
      properties:
        height:
          $ref: '#/components/schemas/RestShrubbery/properties/height'
        radius:
          $ref: '#/components/schemas/RestShrubbery/properties/radius'
        description:
          $ref: '#/components/schemas/RestShrubbery/properties/description'
    RestEditShrubbery:
      type: object
      properties:
        description:
          $ref: '#/components/schemas/RestShrubbery/properties/description'
  parameters:
    kingdomIdInPath:
      name: kingdomId
      in: path
      description: TODO
      required: true
      schema:
        type: string
    knightIdInPath:
      name: knightId
      in: path
      description: TODO
      required: true
      schema:
        type: string
  requestBodies:
    createKnight:
      description: TODO
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/RestCreateKnight'
      required: true
    editKnight:
      description: TODO
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/RestEditKnight'
      required: true
  securitySchemes:
    api_key: 
      type: http
      scheme: bearer
      bearerFormat: token
security:
  - api_key: []
